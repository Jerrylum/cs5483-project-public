# Statistics App

A microservice for generating statistics based on MongoDB task data.

## Development

To start the development server:

```bash
bun run dev
```

## Endpoints

- `GET /api/statistics` - Get task statistics

## Docker

Build and run with Docker Compose:

```bash
docker-compose up --build
```
